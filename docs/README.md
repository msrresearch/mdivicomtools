# Docs (drafts)

These docs are early scaffolding for the `mdivicomtools` public core.

Key topics:
- `docs/openSIDS_v0.1.md` — dataset layout + timebase + annotation pivots (draft)
- `docs/plugin_contract_v0.1.md` — plugin discovery + run interface (draft)
- `docs/public_private_bridge_v0.1.md` — public↔private boundary + install story (draft)

Contributor workflow:
- See `CONTRIBUTING.md` for the minimal public repo policy (branches, PRs, releases, and safety guardrails).
